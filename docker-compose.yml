services:
  frontend:
    build:
      context: ./frontend
    restart: always
    networks:
      - ytdl
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ytdl.rule=(Host(`${HOST}`))"
      - "traefik.http.routers.ytdl.entrypoints=websecure"
      - "traefik.http.routers.ytdl.tls.certresolver=myresolver"

  backend:
    build:
      context: ./backend
    restart: always
    networks:
      - ytdl
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ytdl.rule=(Host(`${HOST}`) && PathPrefix(`/api`))"
      - "traefik.http.routers.ytdl.entrypoints=websecure"
      - "traefik.http.routers.ytdl.tls.certresolver=myresolver"

networks:
  ytdl:
    external: true
